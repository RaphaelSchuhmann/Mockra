server:
  port: 8080

endpoints:
  - id: get
    method: GET
    path: /api/get
    request:
      - username
      - password
    responses:
      - variant: success
        status: 200
        body:
          token: "abc123"